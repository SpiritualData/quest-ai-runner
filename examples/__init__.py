"""Runnable examples for wiring quest-ai-runner to your own Quest backend.

These are reference consumers — they contain NO real keys, ids, emails, or paths; every specific
is read from the environment. See ``examples/README.md``. This package is not installed with the
library (only ``quest_ai_runner*`` is packaged); it lives here to be read, copied, and tested.
"""
